-- Problem: Query the list of employee names from the Employee table in alphabetical order
-- Table: Employee (includes employee_id, name, months, salary)

SELECT name FROM Employee ORDER BY name ;